# Requirements EXTREMAMENTE mínimos para Render 512MB
# Apenas o essencial absoluto para funcionamento

# Flask core (obrigatório)
Flask==2.3.3
Werkzeug==2.3.7

# Google API (essencial para funcionalidade principal)
google-api-python-client==2.110.0
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Requests (essencial)
requests==2.31.0
urllib3==1.26.18

# Gunicorn (servidor produção)
gunicorn==21.2.0

# Utilidades básicas
python-dotenv==1.0.0
bcrypt==4.1.2

# Jinja2 (incluído com Flask, mas explícito para controle de versão)
Jinja2==3.1.2
MarkupSafe==2.1.3

# Timezone handling (essencial)
pytz==2023.3

# REMOVIDOS para economizar memória:
# - pandas (muito pesado - 50MB+)
# - openpyxl (pesado - 10MB+) 
# - psutil (usado apenas para monitoramento)
# - numpy (pesado, dependência do pandas - 20MB+)
# - matplotlib/seaborn (plotting, desnecessário)
# - xlsxwriter (alternativa mais leve se necessário)
# - beautifulsoup4 (parsing HTML, não usado)

# Total estimado: ~15-20MB vs 80MB+ da versão completa
